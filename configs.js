const c_width = 800;
const c_height = 800;

// node config
const space = 1
const size = 5

// puzzle max size
const max_node_col = Math.floor(c_width / (space + size))
const max_node_row = Math.floor(c_height / (space + size))




// line config
const start_line = Math.floor(size/2)